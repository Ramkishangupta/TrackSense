# =============================================================================
# requirements.txt — backend-python
# Dynamic Train ETA Prediction System (SIH 2026)
# Python 3.13 compatible — all packages have pre-built binary wheels.
# Install: pip install -r requirements.txt
# =============================================================================

# --- Web Framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.3

# --- Database ---
sqlalchemy==2.0.41
psycopg2-binary==2.9.10      # Binary wheel — no C compilation needed

# --- Geospatial & H3 ---
h3==4.1.0                    # Uber H3 hexagonal indexing (Python bindings)
geopy==2.4.1                 # Great-circle distance calculations

# --- ML / AI ---
lightgbm==4.6.0              # Baseline ETA model — has py3 wheel, no compilation
pandas==2.2.3                # Pre-built wheel available for Python 3.13
numpy==2.2.4                 # Numerical operations — py3.13 wheel available

# --- LangChain RAG (Phase 6) ---
langchain==0.3.25
langchain-community==0.3.23
langchain-groq==0.3.2        # Groq LLM provider
chromadb==0.6.3              # Vector store for document RAG

# --- Environment & Config ---
python-dotenv==1.1.0         # Load .env files

# --- HTTP Client ---
httpx==0.28.1

# --- CORS & Utilities ---
python-multipart==0.0.20     # For FastAPI file uploads (PDF ingestion in Phase 6)
